# plans/ — 阶段实施计划

每个开发阶段（P0、P1、…，见 [../roadmap.md](../roadmap.md)）一份文件，命名 `Px-<slug>.md`（如 `P0-foundation.md`）。

每份计划包含：
- **目标** + 验收点
- **行为基线**（该阶段要稳定下来的关键行为/分支）
- **任务清单**（todo，实现期勾选）
- **回归测试**（脚本化 mock LLM 覆盖的分支）
- **风险**
- **回顾**（阶段末填：实际做了什么、偏离、遗留）

实现期创建。当前为空。
